# Use IPv4 loopback explicitly — Windows resolves "localhost" to the IPv6
# loopback (::1) first, and the Docker/WSL port relay for that path is
# flaky (intermittently hangs until timeout instead of responding),
# causing sporadic "Failed to fetch" errors on the dashboard.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
